// Churches with a live Planning Center OAuth connection surface (real data,
// not the baked Cornerstone demo snapshot). Add a slug here when a new church
// onboards. One source of truth, imported by the components/modules that need
// to branch between real behavior and demo sample data, or that need to
// trigger a live-data load.
export const LIVE_CHURCHES = ['focal-point-church']
