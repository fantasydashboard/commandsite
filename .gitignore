node_modules
dist
dist-ssr
.env
.env.local
*.log
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea
*.tsbuildinfo

# Supabase CLI metadata (project ref, version pins, local-only state)
supabase/.temp/

# Claude Code per-user settings (personal allowlist, not shared)
.claude/settings.local.json

# Planning Center working directory (member PII, never commit).
# Was scoped to pco-raw/ only, but everything DOWNSTREAM of those pulls carries
# the same names: the generated upload SQL for the roster, serve candidates, and
# the congregation map are all plain congregant lists sitting one `git add -A`
# away from being public. Ignore the whole directory; nothing in it is source.
scratchpad/
